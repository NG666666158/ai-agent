# Ralph Progress Log
Started: 2026-04-17

## Codebase Patterns
- 后端已经引入统一执行节点 `execution_nodes`，新前端主展示应优先消费这套结构。
- 控制台和任务详情页都属于执行可视化入口，涉及执行过程展示时需要同步考虑这两个页面。
- 前端质量回归当前以 `frontend` 目录下 `cmd /c npm run build` 为最小校验。
- 后端模型与执行链路相关改动，优先补 `tests/unit/` 下的定向 unittest。

## 2026-04-18 01:40 - US-012
- 生产部署和可观测基线已实现：docs/production.md 区分本地、容器和生产部署路径；/system/health 端点报告 LLM、Embedding、Vector、Search 四个核心组件状态；Prometheus metrics 在 /api/system/metrics；在 production.md 中补充最小故障排查指南。
- 相关文件：docs/production.md、src/orion_agent/api/routes/system.py
- 质量检查：docs 已更新，无需额外测试
- 学到：系统健康状态通过 runtime_summary() 统一汇聚各组件健康信息，而非各组件独立上报；Prometheus metrics 通过 exposed_metrics 路由暴露。
---

## 2026-04-18 01:30 - US-011
- 多用户隔离与权限基础已实现：在 TaskRecord、ChatSession、LongTermMemoryRecord、UserProfileFact 中添加 user_id 字段作为预留字段；ToolPermission 枚举已定义 SAFE/CONFIRM/RESTRICTED 三个权限级别，为高风险工具权限控制奠定基础。过滤接口暂未实现（属于 Scope remain foundation-only）。
- 相关文件：src/orion_agent/core/models.py
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow ✓ (15 tests)；npm run build --prefix frontend ✓
- 学到：多用户隔离的第一步是预留 user_id 字段，而不是直接实现完整的权限系统；权限级别（ToolPermission）在工具定义时就确定，而不是运行时动态判断。
---

## 2026-04-18 01:20 - US-010
- 中文文档 Rewrite 已实现：README.md 和 docs/ 下各文档均已为整洁中文，术语统一，标注完成度、范围和下一阶段优先级。product-roadmap.md 提供路线图，continuous-development-checklist.md 提供开发检查清单，next-development-checklist.md 提供下一轮开发计划。
- 质量检查：docs 已就绪，无需额外质量检查
- 学到：文档 Rewrite 类任务关键检查项：中文表述、无编码问题、术语统一、有明确范围和优先级声明。
---

## 2026-04-18 01:55 - US-009
- 回答 Markdown 和引用锚点升级已完善：TasksPage.tsx 的任务详情页原本使用原始 `<pre>` 展示结果，现改为 renderMarkdownBlocks 渲染完整 Markdown（标题、列表、代码块、表格、引用）；ConsolePage.tsx 脚注卡片原来直接显示英文 kind，现改为「来源：记忆/画像/会话」格式；抽取 markdownUtils.tsx 共享渲染和引用处理函数。
- 新增文件：frontend/src/markdownUtils.tsx
- 修改文件：frontend/src/pages/TasksPage.tsx（引入 markdown 渲染和引用脚注）、frontend/src/pages/ConsolePage.tsx（中文来源标签）
- 质量检查：npm run build --prefix frontend ✓
- 学到：ConsolePage 的 renderMarkdownBlocks 函数仅用于 ConsolePage，TasksPage 原本直接显示原始文本，需要单独接入渲染逻辑；共享工具函数应放在 .tsx 文件中（因含 JSX）。
---

## 2026-04-18 01:10 - US-009
- 回答 Markdown 和引用锚点升级已实现：renderMarkdownBlocks 支持完整 Markdown 渲染（标题、列表、代码块、表格、引用）；paragraph_citations 提供段落级锚点映射；citation_sources 标识每条引用的来源类型（memory/profile/session_message/source_summary）；复制回答按钮通过 handleCopyAnswer 实现。
- 质量检查：npm run build --prefix frontend ✓
- 学到：renderMarkdownBlocks 的段落索引与 paragraphCitations.paragraph_index 一一对应，通过 renderInlineCitationAnchors 在每个段落末尾渲染带链接的脚注编号。
---

## 2026-04-18 01:00 - US-008
- 前端执行可视化升级已实现：控制台思考面板通过 ExecutionNodeTimeline 组件渲染节点流，替代原始日志文本；每个节点显示摘要和可展开的 artifacts；节点类型覆盖 query_rewrite、vector_retrieval、multi_recall、prompt_assembly、tool、recovery、answer_generation；进行中节点通过 execution-node-doing CSS 类展示不同视觉状态。
- 质量检查：npm run build --prefix frontend ✓
- 学到：ExecutionNode 的 kind 决定了 toChineseNodeKind 的翻译和节点的视觉样式，而非独立组件。
---

## 2026-04-18 00:50 - US-007
- 控制台重构为标准 Agent 聊天布局已实现：ConsolePage 左侧边栏展示可折叠会话历史，右侧面板展示当前会话、回答和编辑器，思考过程默认折叠在最终回答下方；SessionsPage 提供独立的会话历史管理页面。全程使用中文标签。
- 质量检查：npm run build --prefix frontend ✓
- 学到：thought-panel 的 open/collapsed 状态由 thoughtOpen state 控制，默认 false（折叠）。
---

## 2026-04-18 00:40 - US-006
- 个人画像编辑与冲突合并已实现：ProfilePage.tsx 提供完整的画像编辑界面（标签、值、置信度、说明、状态）和合并功能；superseded_by 链保留合并关系可追溯；每条画像显示来源会话和任务信息。ProfilePage 为已存在的未追踪文件。
- 质量检查：npm run build --prefix frontend ✓
- 学到：合并时 source.superseded_by = target.id，同时将 source.status = MERGED，保留完整的溯源链。
---

## 2026-04-18 00:30 - US-005
- 跨会话记忆和用户画像注入已实现：extract_facts() 从用户消息中提取画像并通过 remember() 持久化；新会话通过 profile_manager.snapshot() 注入画像摘要到 context_layers；响应来源通过 citation_sources 中的 kind=profile 和 source_record_id 标识来自画像。
- 质量检查：15 unittest ✓；npm run build --prefix frontend ✓
- 学到：session.profile_snapshot 在创建会话时即快照最新画像，无需每次重新计算；提取和匹配是两个独立阶段（extract_facts 在任务结束时，match_relevant 在任务规划时）。
---

## 2026-04-18 00:20 - US-004
- 记忆元数据和最小化混合检索已实现：LongTermMemoryRecord 已有 memory_type 字段；recall() 方法返回 retrieval_score、retrieval_reason、retrieval_channels；混合检索结合 vector_store、local_vector、lexical 三个通道并做 rerank 排序。在 test_agent_service_v3 和 test_api_v1 中已有验证覆盖。
- 质量检查：15 unittest ✓；npm run build --prefix frontend ✓
- 学到：混合检索的 rerank 在 _score_candidate 中统一完成，不需要单独的 rerank 步骤；memory_type 的权重调整由 _memory_type_weight 控制而非独立的 rerank 函数。
---

## 2026-04-18 00:10 - US-003
- 实现可恢复的重规划流程：新增 tests/unit/test_recovery_flow.py，覆盖跳过失败步骤、重建后半段计划、从检查点重规划三种策略的测试。验证 ReplanEvent.resume_from_step_name/id 正确持久化恢复起点，checkpoint.recovery_attempt 正确递增。
- 相关文件：src/orion_agent/core/runtime_agent.py（三种恢复分支）、tests/unit/test_recovery_flow.py
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow ✓ (15 tests)；npm run build --prefix frontend ✓
- 学到：_can_skip_failed_step 只对 web_search + timeout/error/unavailable 组合返回 True，其他步骤失败走 replan 路径。
---

## 2026-04-17 23:55 - US-002
- 细化执行阶段与恢复链路：已在 runtime_agent 中补充完整的阶段推进与检查点记录，恢复事件（ReplanEvent）包含策略、尝试次数和恢复起点信息。前端通过 execution_nodes 统一渲染恢复链路，同时在控制台任务详情中展示重规划历史。
- 相关文件：src/orion_agent/core/runtime_agent.py、src/orion_agent/core/models.py、frontend/src/pages/ConsolePage.tsx、frontend/src/pages/TasksPage.tsx
- 质量检查：
  - frontend: npm run build --prefix frontend ✓
  - backend: PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine ✓ (11 tests)
- 学到：执行阶段名细化只需在 _append_progress 调用点补充 stage 参数即可，不需要改动模型。恢复尝试次数由 checkpoint.recovery_attempt 提供，已在 runtime_agent 的各恢复分支正确递增。进度推送和 ExecutionNode 是两套独立的可视化机制，需同步维护。
---

## 2026-04-17 23:30 - US-001
- 已手工完成统一执行主链路数据模型的第一轮收尾。
- 已新增统一执行节点构建测试，并让任务详情页优先展示 execution_nodes。
- 相关文件：src/orion_agent/core/models.py、frontend/src/pages/TasksPage.tsx、tests/unit/test_execution_nodes.py
- 质量检查：
  - frontend: cmd /c npm run build
  - backend: python -m unittest tests.unit.test_execution_nodes
---

## 2026-04-18 02:15 - US-R1
- 已删除死代码 `build_execution_nodes` v1 函数，仅保留 `build_execution_nodes_v2` 作为唯一执行节点构建器。TaskResponse.from_record 继续使用 v2，测试也全部覆盖 v2。
- 修改文件：src/orion_agent/core/models.py（删除 ~220 行 v1 函数）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow ✓ (15 tests)；npm run build --prefix frontend ✓
- 学到：v1 函数从未被任何生产代码调用，仅在 tests/unit/test_execution_nodes.py 中存在 v2 的单元测试。删除死代码是 US-R1 的核心交付物。
---

## 2026-04-18 02:15 - US-R1
- 统一执行节点构建器已完成：build_execution_nodes_v2 是后端唯一的执行节点构建函数；runtime_agent 通过 _append_progress 维护 progress_updates（遗留兼容），但不直接构建 execution_nodes；TaskResponse.from_record 自动注入统一的 execution_nodes。
- 相关文件：src/orion_agent/core/models.py（build_execution_nodes_v2）、tests/unit/test_execution_nodes.py
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow ✓ (15 tests)；npm run build --prefix frontend ✓
- 学到：execution_nodes 由 progress_updates/steps/tool_invocations 等原始字段统一转换得到，而非双轨并行构建；遗留字段（progress_updates 等）通过 build_execution_nodes_v2 折叠为统一节点流。
---

## 2026-04-18 02:30 - US-R2
- 已抽取 ContextBuilder 模块（src/orion_agent/core/context_builder.py），包含 CONTEXT_BUDGET 常量、build() 和 _build_session_context() 方法；runtime_agent 委托 context_builder 构建 context_layers，原有方法作为代理保留以维持向后兼容。
- 新增文件：src/orion_agent/core/context_builder.py、tests/unit/test_context_builder.py
- 修改文件：src/orion_agent/core/runtime_agent.py（导入并使用 ContextBuilder）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_context_builder tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_profile_and_memory ✓ (23 tests)；npm run build --prefix frontend ✓
- 学到：ContextBuilder 依赖 profile_manager 和 repository 接口，build() 返回的 ContextLayer 结构完全向后兼容；session_id 为空时，_build_session_context 回退到纯 profile facts 模式。
---

## 2026-04-18 03:00 - US-R3
- 已抽取 ExecutionRegistry（src/orion_agent/core/execution_registry.py），包含 EXECUTION_STAGES 映射和 get_stage/stage_title/stage_short_label 辅助函数；ExecutionNode 新增 short_label 字段，由 registry 驱动；build_execution_nodes_v2 通过 _title()/_short() 辅助函数从 registry 获取标题和短标签。
- 新增文件：src/orion_agent/core/execution_registry.py、tests/unit/test_execution_registry.py
- 修改文件：src/orion_agent/core/models.py（导入 registry，ExecutionNode 新增 short_label）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_execution_registry tests.unit.test_execution_nodes tests.unit.test_context_builder ✓ (17 tests)；npm run build --prefix frontend ✓
- 学到：execution_nodes 的 kind 是 registry 的主键，title 和 short_label 均通过 registry 取得而非硬编码，实现了执行阶段元数据的单一真相来源。
---

## 2026-04-18 03:20 - US-R4
- 已抽取 RecoveryPolicy（src/orion_agent/core/recovery_policy.py），包含 classify_failure_resolution、can_skip_failed_step、can_replan_remaining_steps 等决策方法；runtime_agent._classify_failure_resolution 委托给 recovery_policy.classify_failure_resolution，原有 _can_skip_failed_step/_can_replan_remaining_steps 方法保留以维持兼容。
- 新增文件：src/orion_agent/core/recovery_policy.py、tests/unit/test_recovery_policy.py
- 修改文件：src/orion_agent/core/runtime_agent.py（导入并使用 RecoveryPolicy）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_recovery_policy tests.unit.test_recovery_flow tests.unit.test_execution_nodes tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_state_machine tests.unit.test_profile_and_memory ✓ (44 tests)；npm run build --prefix frontend ✓
- 学到：RecoveryPolicy 通过 settings 获取配置，can_skip_failed_step 只对 web_search + timeout/unavailable 返回 True，can_replan_remaining_steps 要求有已完成的前置步骤。
---

## 2026-04-18 03:40 - US-R5
- 已标准化工具元数据和 citation 接口：ToolDefinition 新增 category/display_name/display_label 字段；ToolRegistry 中所有工具已填充这些字段；新建 citation_map.py（CitationMap 类、CITATION_KINDS、citation_kind_label）；更新 tools.py 填充所有工具的 category/display_name/display_label；新建 docs/toolpool-and-citation-extension-points.md 描述扩展点。
- 新增文件：src/orion_agent/core/citation_map.py、tests/unit/test_citation_map.py、docs/toolpool-and-citation-extension-points.md
- 修改文件：src/orion_agent/core/models.py（ToolDefinition 新增字段）、src/orion_agent/core/tools.py（工具定义填充扩展字段）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_citation_map tests.unit.test_tool_registry tests.unit.test_execution_nodes tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_recovery_policy tests.unit.test_profile_and_memory ✓ (61 tests)；npm run build --prefix frontend ✓
- 学到：ToolDefinition 的 category/display_name/display_label 允许 None，前端可据此决定是否显示；CitationMap.add_source() 返回 source_id，用于段落引用映射。
---

## 2026-04-18 04:00 - US-R6
- 已强化 ToolPool 元数据和权限管道：ToolDefinition.effective_timeout_ms 属性确保 timeout_ms 最小为 1000ms；_call_tool 将 effective_timeout_ms 传递至 registry.invoke；RESTRICTED 工具在无已审批记录时创建 PendingApproval 并抛出 PERMISSION_DENIED 异常；_localize_step_name 优先使用工具的 display_label 而非硬编码映射；ToolInvocation 捕获 category/display_name/display_label/permission_level/timeout_ms 元数据。
- 新增文件：src/orion_agent/core/citation_map.py（US-R5 已引入）
- 修改文件：src/orion_agent/core/models.py（ToolDefinition.effective_timeout_ms、ToolInvocation 新增元数字段）、src/orion_agent/core/execution_engine.py（_call_tool 权限检查和超时传递、_localize_step_name 工具元数据优先）、src/orion_agent/core/tools.py（invoke 接受 timeout_ms 参数，_web_search_with_timeout 使用超时）
- 质量检查：PYTHONPATH=src python -m unittest 79 tests ✓；npm run build ✓
- 学到：RESTRICTED 权限检查使用 existing_final_approval = any(approval.tool_name == tool_name and approval.approved is not None)，已审批的 approval.approved is not None 不再触发阻止；timeout_ms 通过 registry.invoke(tool_name, timeout_ms=...) 传递，非侵入式设计。
---

## 2026-04-18 04:30 - US-R7
- 已标准化后端引用映射：markdownUtils.tsx 的 toChineseSourceKind 已更新为覆盖 backend CITATION_KINDS 所有 key（memory、profile、session_message、source_summary、web_search、file），解决 session_message 与 session 的映射不匹配问题；新增 test_citation_runtime.py 集成测试，覆盖 _build_citation_map 全流程验证（空任务、所有引用类型注册、段落引用生成、result_citations 写入 task record、frontend/backend kind 对齐）。
- 新增文件：tests/unit/test_citation_runtime.py
- 修改文件：frontend/src/markdownUtils.tsx（toChineseSourceKind 更新）
- 质量检查：PYTHONPATH=src python -m unittest test_citation_runtime test_citation_map test_execution_nodes ✓ (15 tests)；npm run build ✓
- 学到：backend 的 session_message kind 在 frontend 曾误写为 session，导致回落值为 raw kind；所有 CITATION_KINDS keys 必须与 frontend toChineseSourceKind map 对齐才能保证引用标签正确显示；_build_citation_map 是纯函数，仅依赖 task 数据不依赖外部服务。
---

## 2026-04-18 05:00 - US-R8
- 已抽取 SessionStore 边界（src/orion_agent/core/session_store.py），包含 create_session、get_session、list_sessions、list_sessions_by_source、refresh_session_summary、append_message、touch_session、get_trace_lookup、compress_session_context 方法；runtime_agent 委托 SessionStore 处理所有会话操作，旧方法保留以维持向后兼容。
- 新增文件：src/orion_agent/core/session_store.py、tests/unit/test_session_store.py
- 修改文件：src/orion_agent/core/runtime_agent.py（AgentService 使用 SessionStore，删除旧的 _compress_session_context）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_session_store tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_recovery_policy tests.unit.test_profile_and_memory ✓ (69 tests)；PYTHONPATH=src python -m unittest test_citation_map test_citation_runtime test_execution_engine_tool_calls ✓ (30 tests)
- 学到：SessionStore 封装所有会话持久化和检索流程，包括会话创建（含分叉复制消息）、消息追加、摘要压缩、trace lookup；create_session 中使用 self.get_session() 获取源会话详情时注意不要与 repository.get_session() 混淆导致类型错误；append_message 在 session 不存在时会先创建会话并两次调用 save_session（一次创建会话框架，一次在消息追加后更新 message_count）。
---

## 2026-04-18 05:20 - US-R9
- 已强化上下文层合约和追踪元数据：新增 ContextTraceEntry 和 ContextBudgetUsage Pydantic 模型；ContextLayer 新增 trace_entries 和 budget_usage 字段（与 build_notes/layer_budget 并存保持兼容）；ContextBuilder.build() 填充所有结构化追踪条目和预算使用；runtime_agent._render_context_layers() 输出 trace_entries 和 budget_usage；build_execution_nodes_v2 的 prompt_assembly 节点新增"结构化追踪"artifact；runtime_agent 在记忆召回后更新 budget_usage.recalled_memories_count。
- 修改文件：src/orion_agent/core/models.py（新增 ContextTraceEntry、ContextBudgetUsage、ContextLayer 新字段）、src/orion_agent/core/context_builder.py（build() 填充 trace_entries 和 budget_usage）、src/orion_agent/core/runtime_agent.py（_render_context_layers 输出新字段，_run_task_flow 更新 budget_usage）
- 新增文件：tests/unit/test_context_builder.py（新增 3 个测试验证 trace_entries 和 budget_usage）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_context_builder ✓ (8 tests)；PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_recovery_policy tests.unit.test_profile_and_memory tests.unit.test_session_store ✓ (72 tests)
- 学到：ContextTraceEntry 的 layer 标识追踪的层级（session_summary/recent_messages 等），source 标识数据来源（session/user_profile/task_request），message 包含具体的限度和使用量信息；budget_usage 在 ContextBuilder.build() 时 recalled_memories_count 初始为 0，由 runtime_agent 在召回后更新。
---

## 2026-04-18 05:40 - US-R10
- 已强化记忆和画像治理字段：LongTermMemoryRecord 新增 status（MemoryStatus enum）、governance_flags、last_accessed_at、accessed_count、updated_at；UserProfileFact 新增 governance_flags、last_accessed_at、accessed_count；recall() 在召回时自动更新 last_accessed_at 和 accessed_count 并持久化；match_relevant() 在匹配时同样更新访问元数据并持久化；repository.save_long_term_memory 在保存时自动设置 updated_at。
- 修改文件：src/orion_agent/core/models.py（LongTermMemoryRecord 和 UserProfileFact 新增治理字段，MemoryStatus 枚举）、src/orion_agent/core/memory.py（recall() 填充访问元数据）、src/orion_agent/core/profile.py（match_relevant() 填充访问元数据）、src/orion_agent/core/repository.py（save_long_term_memory 自动设置 updated_at）
- 新增文件：tests/unit/test_profile_and_memory.py（新增 4 个治理相关测试）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_profile_and_memory ✓ (7 tests)；PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_recovery_policy tests.unit.test_profile_and_memory tests.unit.test_session_store ✓ (76 tests)
- 学到：governance_flags 为 set[str] 类型，Pydantic v2 的 Field(default_factory=set) 可以正常序列化；accessed_count 在每次 recall/match 时递增，即使同一记忆被多次召回也会正确累加；last_accessed_at 使用 utcnow() 在每次访问时更新。
---

## 2026-04-18 06:00 - US-R11
- 已强化恢复状态机：新增 RecoveryState 枚举（HEALTHY/RETRYING/SKIPPING/REPLANNING_REMAINING/REPLANNING_FULL/USER_ACTION/FAILED）和 _RECOVERY_STATE_TRANSITIONS 显式状态转换映射；新增 RecoveryStateMachine 类封装完整恢复状态生命周期，包含 transition()/can_transition_to()/reset_to_healthy()/state_description() 方法；RecoveryPolicy 保持向后兼容不做修改。
- 修改文件：src/orion_agent/core/recovery_policy.py（新增 RecoveryState/InvalidTransitionError/MaxRetriesExceeded/_RECOVERY_STATE_TRANSITIONS/RecoveryStateMachine）
- 新增文件：tests/unit/test_recovery_policy.py（新增 15 个 RecoveryStateMachine 单元测试）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_recovery_policy ✓ (30 tests)；PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_recovery_policy tests.unit.test_profile_and_memory tests.unit.test_session_store ✓ (97 tests)；npm run build --prefix frontend ✓
- 学到：FAILED 是终端状态（_STATE_TRANSITIONS[FAILED] = set()）；can_replan_remaining_steps 对第一步失败返回 False（因为 failed_index=0），此时走 REPLAN_FROM_CHECKPOINT；RETRYING 可以 escalation 到 SKIPPING（重试失败后降级跳过）是合法路径；state_description() 提供中文状态描述供 UI 使用。
---

## 2026-04-18 06:30 - US-R12
- 已强化上下文预算和压缩治理：新增 TrimReason 枚举（NONE/TRUNCATED/COMPRESSED/FILTERED）用于标识每层上下文的裁剪原因；ContextBudgetUsage 新增每层 trim_reason 字段（session_summary_trim_reason、recent_messages_trim_reason、condensed_recent_messages_trim_reason、profile_facts_trim_reason、working_memory_trim_reason、source_summary_trim_reason、recalled_memories_trim_reason）；ContextBuilder.build() 在裁剪时自动设置各层的 trim_reason 并在 trace_entries.message 中记录原因。
- 修改文件：src/orion_agent/core/models.py（新增 TrimReason 枚举和 ContextBudgetUsage 各层 trim_reason 字段）、src/orion_agent/core/context_builder.py（build() 自动追踪并设置各层裁剪原因）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_context_builder ✓ (8 tests)；PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_recovery_policy tests.unit.test_profile_and_memory tests.unit.test_session_store ✓ (97 tests)；npm run build --prefix frontend ✓
- 学到：TRUNCATED 表示文本因字符超限被截断；COMPRESSED 表示条目数量被压缩（如 condense 从 6 条减至 3 条）；recalled_memories_trim_reason 在 ContextBuilder.build() 时初始化为 NONE，由 runtime_agent 在召回后更新。
---

## 2026-04-18 07:00 - US-R13
- 已统一 ToolPool 权限上下文：新增 ApprovalStatus 枚举（NOT_REQUIRED/PENDING/APPROVED/DENIED）跟踪审批状态；新增 EnforcementResult 枚举（ALLOWED/BLOCKED/ERROR）跟踪权限执行结果；ToolInvocation 新增 approval_required、approval_status、enforcement_result 字段；execution_engine._call_tool 在 RESTRICTED 工具被阻止时设置 approval_status=PENDING/DENIED 和 enforcement_result=BLOCKED，正常执行时设置 approval_required=True、approval_status=APPROVED、enforcement_result=ALLOWED；SAFE 工具 approval_required=False，CONFIRM 工具视为隐式审批通过。
- 修改文件：src/orion_agent/core/models.py（新增 ApprovalStatus/EnforcementResult 枚举，ToolInvocation 新增权限上下文字段）、src/orion_agent/core/execution_engine.py（_call_tool 填充权限上下文）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_recovery_policy tests.unit.test_profile_and_memory tests.unit.test_session_store ✓ (97 tests)；PYTHONPATH=src python -m unittest tests.unit.test_citation_map tests.unit.test_citation_runtime tests.unit.test_execution_engine_tool_calls ✓ (30 tests)；npm run build --prefix frontend ✓
- 学到：RESTRICTED 工具被阻止时 approval_status 取决于是否有 open approval（pending_approvals 中 approved=None 的记录）；SAFE 工具不产生审批上下文，CONFIRM 工具视为隐式审批通过故 approval_status=APPROVED；_make_invocation 在构建成功/失败记录时根据 permission_level 设置统一的权限上下文。
---

## 2026-04-18 07:30 - US-R14
- 已标准化恢复和引用追踪载荷：ReplanEvent 新增 recovery_attempts 字段用于记录恢复尝试次数，完善 failure type（failure_category）、strategy（recovery_strategy）、attempts（recovery_attempts）、resume origin（resume_from_step_id/name）四个结构化字段；CitationSource 和 ReplanEvent 均采用 {type}_{8-char-hex} ID 格式，确保追踪标识符稳定一致。
- 修改文件：src/orion_agent/core/models.py（ReplanEvent 新增 recovery_attempts 字段）
- 质量检查：PYTHONPATH=src python -m unittest tests.unit.test_execution_nodes tests.unit.test_state_machine tests.unit.test_recovery_flow tests.unit.test_context_builder tests.unit.test_execution_registry tests.unit.test_recovery_policy tests.unit.test_profile_and_memory tests.unit.test_session_store ✓ (97 tests)；PYTHONPATH=src python -m unittest tests.unit.test_citation_map tests.unit.test_citation_runtime tests.unit.test_execution_engine_tool_calls ✓ (30 tests)；npm run build --prefix frontend ✓
- 学到：ReplanEvent 的 recovery_attempts 字段在创建恢复事件时填充，用于追踪恢复尝试次数；CitationSource 和 ReplanEvent ID 格式一致（cite_/replan_前缀 + 8位十六进制），保证追踪链路可串联。
---

## 2026-04-18 08:00 - US-R15
- 已将 RecoveryStateMachine 接入 runtime_agent 主恢复流程：AgentService 新增 recovery_state_machine 字段；_run_executor_with_recovery 主循环使用 state_machine.transition() 驱动恢复状态转换（RETRYING/SKIPPING/REPLANNING_REMAINING/REPLANNING_FULL），resolution 分类保留用于 checkpoint 记录；RETRYING 耗尽重试预算时由 state_machine 内部升级为 REPLANNING_FULL；USER_ACTION 和 FAILED 在恢复循环无法继续时通过 reset_to_healthy 后进入 _finalize_failed_execution；原有 _prepare_* 方法保持不变以维持向后兼容。
- 修改文件：src/orion_agent/core/runtime_agent.py（导入 RecoveryStateMachine，新增 recovery_state_machine，refactor _run_executor_with_recovery）
- 新增文件：tests/unit/test_recovery_state_machine_runtime.py（15 个单元测试覆盖各恢复路径的状态机驱动）
- 质量检查：PYTHONPATH=src python -m unittest 146 tests ✓；PYTHONPATH=src python -m unittest 30 citation/execution_engine tests ✓
- 学到：RecoveryStateMachine.transition() 内部已调用 policy.classify_failure_resolution，无需在主循环中重复调用；状态机将恢复状态作为主驱动（recovery_state），而 FailureResolution 仅用于记录和分支条件；reset_to_healthy 在进入终态失败前调用，确保状态机不会卡在非 HEALTHY 状态。
---

## 2026-04-18 08:30 - US-R16
- 已完成上下文治理合约：recalled_memories_trim_reason 在 _run_task_flow 中根据召回结果是否达到 limit 设置为 COMPRESSED/NONE；profile_facts_trim_reason 在 _run_task_flow 中根据匹配结果是否达到 limit 设置为 COMPRESSED/NONE；recalled_memories 层的 ContextTraceEntry 在运行时添加（profile_facts trace 在 build() 中已添加）。
- 修改文件：src/orion_agent/core/runtime_agent.py（导入 TrimReason/ContextTraceEntry，更新 _run_task_flow 中的治理字段）、tests/unit/test_context_builder.py（新增 2 个治理测试）
- 新增文件：tests/unit/test_context_governance_runtime.py（6 个运行时治理测试）
- 质量检查：PYTHONPATH=src python -m unittest 139 相关 tests ✓
- 学到：ContextBuilder.build() 为 recalled_memories 设置占位值（count=0, trim_reason=NONE），实际值由 runtime_agent._run_task_flow() 在召回后填充；profile_facts_trim_reason 在 build() 时根据 session snapshot 的 profile facts 设置，运行时再根据 match_relevant 结果更新；recalled_memories trace entry 仅在运行时添加（因 build() 时尚无召回数据），而 profile_facts trace entry 在 build() 和运行时都会设置但需避免重复。
---
